mutation EditGuildRule($id: BigInt!, $patch: GuildRulePatch!) {
    updateGuildRule(input: { id: $id, patch: $patch }) {
        guildRule {
            actions
            conditions
            createdAt
            enabled
            id
            name
            setId
            trigger
            updatedAt
        }
    }
}
