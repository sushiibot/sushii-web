query GuildRule($id: BigInt!) {
    guildRule(id: $id) {
        actions
        conditions
        enabled
        id
        createdAt
        name
        setId
        trigger
        updatedAt
    }
}
