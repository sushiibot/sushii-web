query GuildTags($guildId: BigInt!) {
    tags(condition: { guildId: $guildId }) {
        nodes {
            content
            created
            ownerId
            tagName
            useCount
        }
        totalCount
    }
}
